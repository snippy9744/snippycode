<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SnippySeatApplication"
        android:allowBackup="true"
        android:icon="@mipmap/snippy_seat_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/snippy_seat_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SnippySeat"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SnippySeat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="booking"
                    android:scheme="snippyseat" />
                <data
                    android:host="salon"
                    android:scheme="snippyseat" />
                <data
                    android:host="seller"
                    android:pathPrefix="/dashboard"
                    android:scheme="snippyseat" />
            </intent-filter>
        </activity>
        <service
            android:name=".core.notifications.SnippyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
